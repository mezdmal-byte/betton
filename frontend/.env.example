# Leave empty to call relative paths through the Vite dev proxy.
VITE_API_BASE_URL=

# FastAPI origin used only by `npm run dev` proxy. Do not put tunnel URLs here in commits.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
