name: P2P journal PostgreSQL

on:
  pull_request:
  push:
    branches: [main, test/postgres-p2p-ledger]

permissions:
  contents: read

jobs:
  sqlite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: pip install -r requirements.txt
      - name: SQLite pytest
        run: python -m pytest -q tests --ignore=tests/test_p2p_journal_postgres.py

  postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: betton
          POSTGRES_PASSWORD: betton_test_only
          POSTGRES_DB: betton_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U betton -d betton_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      BETTON_TEST_DATABASE_URL: postgresql+psycopg2://betton:betton_test_only@localhost:5432/betton_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: pip install -r requirements.txt
      - name: PostgreSQL journal tests
        run: python -m pytest -q tests/test_p2p_journal_postgres.py tests/test_integer_money.py tests/test_money_migration.py tests/test_beta_e2e.py
